# MetaDataScraper 严格评审与重构建议

本文从“优秀元数据下载器 / 媒体库管理器”的标准审视当前项目。结论先说在前面：这个项目已经具备可用雏形，但离“可放心批量处理真实媒体库”的工具还有明显距离。最大问题不是某个 API key 或某个搜索源，而是缺少可信匹配、可回滚整理、确定性输出、清晰状态反馈和完整测试这五根骨架。

如果允许大刀阔斧改动，建议不要继续在现有线性流程上补丁式修小洞，而是重构成“扫描 -> 候选生成 -> 用户确认/规则判定 -> 计划生成 -> 执行 -> 校验/回滚”的事务型媒体管理系统。

## 一句话诊断

当前项目更像“自动化脚本 + Web 面板”，还不是“可靠媒体库管理器”。

它能搜索、能写 NFO、能移动文件、能下载图片，但这些动作之间缺乏强约束和强可解释性。对真实媒体库来说，最危险的不是失败，而是“自信地整理错”。例如把 `Front Innocent` 匹配到无关纪录片，这类错误一旦进入重命名、NFO 写入和图片下载，会污染媒体库，后续修复成本很高。

## P0：必须优先解决的问题

### 1. 匹配可信度不足，错误匹配风险过高

当前匹配主要依赖 TMDB 搜索结果、标题相似度、少量语言启发式和 Tavily fallback。对日漫、里番、罗马音、中文译名、字幕组命名来说，这远远不够。

典型风险：

- 罗马音标题和 TMDB 中文/日文标题相似度极低，正确候选会被拒绝。
- 英文短标题容易和无关英文作品误匹配。
- 文件名中字幕组、压制参数、清晰度、音轨信息会污染查询词。
- 年份缺失时，系统缺少强校验。
- 多集散装文件可能被错误分组。

建议：

- 引入 `Candidate` 数据结构，保存所有候选、来源、置信分、命中理由、拒绝理由。
- 匹配分数拆成多个维度：标题相似度、别名命中、年份匹配、媒体类型匹配、集数范围匹配、原始语言匹配、外部搜索佐证。
- 对低置信候选强制进入“待确认”，不要自动整理。
- 对英文短标题提高阈值，至少要求 token 覆盖率或年份佐证。
- 对 CJK/罗马音标题支持别名表、TMDB alternative_titles、用户自定义映射。
- 每次匹配都输出可读解释：为什么选它，为什么拒绝其他候选。

### 2. 原地整理缺少事务和回滚

当前移动/重命名一旦执行，缺少完整的操作计划、冲突预检、回滚日志和恢复入口。这对媒体库管理器是硬伤。

建议：

- 执行前生成 `plan.json`，列出每个文件的 source、destination、动作、原因、冲突状态。
- 所有修改先 dry-run 生成计划，用户确认后执行。
- 执行时写 `operation-log.jsonl`，逐条记录成功/失败。
- 支持 `rollback`，按日志反向移动文件和删除生成物。
- 遇到目标文件已存在时，不能只 warning，需要进入冲突策略：跳过、覆盖、追加后缀、合并、人工确认。
- 原地整理默认应保守，批量整理默认应使用 copy 或 staging 目录。

### 3. 图片下载策略仍不够专业

现在已经比之前更好，主图和 Extra 图能编号保存，但还没有达到优秀刮削器标准。

当前仍需改进：

- 图片选择只是取第一个，没有按语言、评分、分辨率、尺寸比例、vote_count 排序。
- `banner.jpg` 是从 fanart 复制，不是真 banner。对 Emby/Jellyfin 可用，但语义上应明确。
- 图片下载结果没有写入 manifest，无法知道来源 URL、尺寸、语言、下载时间。
- 没有图片完整性校验，例如 0 字节、错误 HTML、损坏图片。
- `Extra` 目录结构和媒体服务器兼容性需要明确策略。

建议：

- 增加 `artwork_policy` 配置：
  - preferred_languages
  - poster_count
  - backdrop_count
  - logo_count
  - min_width/min_height
  - prefer_language_then_vote
  - overwrite / keep / refresh_missing
- 下载前先排序：语言匹配 > vote_average/vote_count > 分辨率 > aspect ratio。
- 每个媒体目录写 `artwork-manifest.json`。
- 对主图生成明确说明：`banner.jpg` 如由 backdrop 派生，应记录 `derived_from: fanart.jpg`。
- 下载后校验 MIME、尺寸和文件大小。

### 4. 前端状态反馈不够可信

当前 UI 已能显示任务卡片，但任务状态依赖日志解析。日志解析是脆弱的：日志文字一改，前端状态就坏。优秀管理器不能靠正则解析日志来驱动状态。

建议：

- 后端提供结构化事件流，而不是纯日志：
  - `task.created`
  - `candidate.found`
  - `candidate.rejected`
  - `plan.generated`
  - `file.moved`
  - `artwork.downloaded`
  - `nfo.written`
  - `task.failed`
  - `task.completed`
- WebSocket 发送 JSON event，日志只作为辅助。
- 每个 task 有稳定 ID，不要用线程名或标题推断。
- UI 显示失败原因、候选列表、重试参数和实际请求模式。
- RUN 后应立即显示“正在扫描路径”，而不是等识别到候选才出现任务卡。

### 5. 配置和密钥管理不专业

当前配置散落在 `config/config.yaml`、`client/config/config.yaml`、环境变量、前端设置页之间，容易产生“写了但不生效”的错觉。

建议：

- 只保留一个后端真实配置源：`config/config.yaml`。
- 前端设置页通过 API 读写后端配置，不保留 `client/config/config.yaml` 这种易混淆文件。
- 密钥字段返回前端时默认 mask，例如 `tvly-dev-****ionRt`。
- 保存密钥时支持“不改动旧密钥”的占位值。
- 配置 schema 用 Pydantic 校验，保存前给出错误提示。
- 敏感配置不要进入 git。当前已有真实 token/key 写入配置，建议后续迁移到 `.env` 或本地 secret store。

## P1：重要架构改造

### 1. 分离“搜索”和“处理”

现在 pipeline 一路从搜索跑到写文件、下载图。建议拆成：

- Scanner：只负责发现媒体项目。
- Parser：解析文件名、集数、年份、字幕组信息。
- Matcher：生成候选并评分。
- Planner：生成整理计划。
- Executor：执行文件操作和写入。
- ArtworkService：下载图片。
- NfoService：写 NFO。
- StateStore：记录任务、候选、计划、结果。

这样可以做到先展示候选和计划，再执行危险操作。

### 2. 文件名解析器需要升级为规则系统

当前 `FilenameParser` 是一组正则和启发式，维护会越来越痛苦。

建议：

- 把解析结果建模为：
  - release_group
  - title
  - year
  - season
  - episode
  - resolution
  - source
  - codec
  - audio
  - language
  - edition
- 每条规则有优先级和测试样例。
- 引入 fixtures，覆盖字幕组命名、动漫命名、电影命名、剧集命名、散装文件。
- 对无法解析的文件进入 unknown bucket，不要乱猜。

### 3. 建立本地匹配数据库

真实媒体库经常重复处理同一批标题。每次重新搜索会慢且不稳定。

建议：

- 建立 SQLite 表：
  - parsed_items
  - candidates
  - user_confirmed_matches
  - rejected_matches
  - operations
  - artwork_assets
- 用户确认过的标题映射下次直接使用。
- 错误匹配可加入黑名单。

### 4. NFO 策略需要明确兼容目标

当前 NFO 生成面向 Kodi/Emby/Jellyfin，但没有明确矩阵。

建议：

- 明确支持目标：Jellyfin / Emby / Kodi。
- 为 movie、tvshow、season、episode 分别建立 snapshot tests。
- NFO 中 uniqueid、tmdbid、premiered、aired、thumb、fanart、actor、genre、studio 等字段统一策略。
- 不要生成无法与视频文件对应的 episode NFO。

### 5. 多线程处理需要更安全

当前多个任务并发共享 pipeline、日志、文件系统，容易产生竞态。

建议：

- 每个 task 独立上下文。
- 对同一媒体目录加锁。
- 文件写入使用临时文件 + atomic rename。
- 下载图片和写 NFO 可并发，但移动文件应受计划顺序控制。
- 任务取消要能停止下载、停止写入、保存半完成状态。

## P2：用户体验改进

### 1. 审计模式应该成为主工作流

优秀工具默认不应该直接整理，而应默认：

1. 扫描
2. 展示候选和置信度
3. 展示计划
4. 用户确认
5. 执行

当前“审计”和“整理”像两个模式，但审计结果不能自然转成确认计划。

建议：

- 审计完成后每个任务显示：
  - 推荐匹配
  - 备选匹配
  - 置信度
  - 将要移动/重命名的文件
  - 将写入的 NFO 和图片
  - “确认执行”按钮

### 2. 失败处理要可操作

当前失败卡片只有 retry，不够。

建议：

- 失败卡片显示：失败阶段、错误原因、查询词、搜索源、候选拒绝原因。
- 支持手动输入 TMDB ID 后重试。
- 支持修改媒体类型后重试。
- 支持把标题加入别名映射。
- 支持跳过并记录。

### 3. 设置页应少而准

现在设置项混在一起，且部分配置是否生效不明显。

建议分组：

- API Keys：TMDB、Tavily、OMDB、LLM。
- Scraping：语言、搜索源优先级、置信阈值。
- Artwork：主图、额外图、限额、覆盖策略。
- Organization：命名模板、冲突策略、是否创建 Season 文件夹。
- Safety：默认 dry-run、执行前确认、回滚日志保留。

## 建议的重构路线

### 第一阶段：止血

- 引入结构化任务事件，前端不再解析日志。
- 修复 Google/Tavily/TMDB 搜索源命名和配置一致性。
- 让图片下载 manifest、限额、覆盖策略稳定生效。
- 所有 KeyError 类配置问题用 schema 默认值解决。
- 所有失败展示明确错误阶段和原因。

### 第二阶段：可信匹配

- 新建 Candidate 模型和评分器。
- 保存候选、拒绝原因、来源。
- 支持手动确认和 TMDB ID override。
- 建立用户确认映射库。
- 引入解析 fixtures 和匹配单元测试。

### 第三阶段：事务化整理

- 先生成 plan，再执行。
- 支持冲突策略。
- 支持 operation log 和 rollback。
- 所有文件操作使用 atomic 写入。
- 原地整理默认需要二次确认。

### 第四阶段：媒体服务器级输出

- Jellyfin/Emby/Kodi NFO snapshot tests。
- Artwork policy 完整化。
- 支持按媒体服务器模板输出。
- 支持刷新缺失图片、只补 NFO、只重命名、只下载图片等细粒度任务。

## 推荐的新目录结构

```text
src/
  domain/
    models.py
    events.py
    plans.py
  scan/
    scanner.py
    filename_parser.py
  match/
    tmdb_matcher.py
    tavily_matcher.py
    scorer.py
    aliases.py
  plan/
    planner.py
    conflict.py
  execute/
    executor.py
    rollback.py
  metadata/
    nfo_writer.py
    artwork_service.py
  server/
    api.py
    websocket_events.py
    settings.py
  storage/
    db.py
    repositories.py
```

## 后端完整设计蓝图

后端应该从“一个 FastAPI 包住脚本”重构为“任务驱动的媒体管理服务”。核心目标是：任何文件系统变更都必须可计划、可解释、可追踪、可回滚。

### 后端分层

推荐分层如下：

```text
backend/
  api/
    routes_tasks.py
    routes_settings.py
    routes_filesystem.py
    routes_matches.py
    routes_plans.py
    routes_artwork.py
    routes_events.py
  domain/
    media_item.py
    candidate.py
    match_result.py
    organization_plan.py
    operation.py
    task.py
    artwork.py
    settings.py
  services/
    scan_service.py
    parse_service.py
    match_service.py
    plan_service.py
    execute_service.py
    rollback_service.py
    artwork_service.py
    nfo_service.py
    settings_service.py
  adapters/
    tmdb_client.py
    tavily_client.py
    filesystem_client.py
  storage/
    db.py
    task_repository.py
    match_repository.py
    plan_repository.py
    operation_repository.py
  events/
    bus.py
    schemas.py
    websocket.py
```

### 领域模型

后端应先定义稳定模型，再让 UI 和流程围绕模型工作。

#### MediaItem

```python
class MediaItem(BaseModel):
    id: str
    source_path: str
    kind: Literal["directory", "loose_group", "file"]
    parsed_title: str | None
    parsed_year: int | None
    parsed_media_type: Literal["movie", "tv"] | None
    season: int | None
    episodes: list[int]
    files: list[MediaFile]
    parse_confidence: float
    parse_notes: list[str]
```

#### Candidate

```python
class Candidate(BaseModel):
    id: str
    media_item_id: str
    source: Literal["tmdb", "tavily", "manual", "local_nfo"]
    tmdb_id: int
    media_type: Literal["movie", "tv"]
    title: str
    original_title: str | None
    year: int | None
    poster_url: str | None
    score: float
    score_breakdown: dict[str, float]
    accept_reasons: list[str]
    reject_reasons: list[str]
```

#### OrganizationPlan

```python
class OrganizationPlan(BaseModel):
    id: str
    task_id: str
    media_item_id: str
    candidate_id: str
    confidence: Literal["high", "medium", "low"]
    requires_confirmation: bool
    operations: list[FileOperation]
    nfo_outputs: list[NfoOutput]
    artwork_outputs: list[ArtworkOutput]
    conflicts: list[PlanConflict]
```

#### FileOperation

```python
class FileOperation(BaseModel):
    id: str
    action: Literal["move", "copy", "rename", "write", "delete", "mkdir"]
    source: str | None
    destination: str
    status: Literal["planned", "running", "done", "failed", "rolled_back"]
    conflict_strategy: Literal["skip", "overwrite", "suffix", "merge", "error"]
    rollback_action: dict | None
```

### 任务生命周期

任务不应只有 `running true/false`，应有明确状态机。

```text
created
  -> scanning
  -> parsed
  -> matching
  -> planning
  -> awaiting_confirmation
  -> executing
  -> verifying
  -> completed

失败分支：
  -> failed
  -> retrying

取消分支：
  -> cancel_requested
  -> cancelled

回滚分支：
  -> rollback_requested
  -> rolling_back
  -> rolled_back
```

### 后端 API 设计

当前 `/api/tasks/start` 一步跑到底，太粗。建议改成多个明确 API。

#### 任务

```http
POST /api/tasks
GET  /api/tasks
GET  /api/tasks/{task_id}
POST /api/tasks/{task_id}/cancel
POST /api/tasks/{task_id}/retry
DELETE /api/tasks/{task_id}
```

`POST /api/tasks` 请求体：

```json
{
  "input_dir": "/Volumes/WorkStation/本地文件",
  "mode": "audit",
  "media_type": "auto",
  "search_mode": "smart",
  "workers": 4,
  "use_local_nfo": false
}
```

#### 扫描与候选

```http
GET  /api/tasks/{task_id}/items
GET  /api/items/{item_id}/candidates
POST /api/items/{item_id}/candidates/refresh
POST /api/items/{item_id}/match
POST /api/items/{item_id}/manual-match
```

手动匹配：

```json
{
  "tmdb_id": 324120,
  "media_type": "tv",
  "remember_alias": true
}
```

#### 计划

```http
POST /api/tasks/{task_id}/plan
GET  /api/tasks/{task_id}/plan
PATCH /api/plans/{plan_id}/operations/{operation_id}
POST /api/plans/{plan_id}/confirm
POST /api/plans/{plan_id}/execute
```

#### 回滚

```http
GET  /api/tasks/{task_id}/operations
POST /api/tasks/{task_id}/rollback
```

#### 设置

```http
GET  /api/settings
PATCH /api/settings
POST /api/settings/test-connectivity
```

设置接口必须返回 mask 后的 key：

```json
{
  "tmdb": { "api_key": "eyJ...JISw", "configured": true },
  "tavily": { "api_key": "tvly-dev-****ionRt", "configured": true }
}
```

保存时允许：

```json
{
  "tavily": { "api_key": "__KEEP_EXISTING__" }
}
```

### 事件协议

前端不应解析日志。后端 WebSocket 应发送结构化事件。

```json
{
  "id": "evt_01",
  "task_id": "task_123",
  "item_id": "item_456",
  "type": "candidate.rejected",
  "timestamp": "2026-06-03T16:30:00+08:00",
  "payload": {
    "candidate_id": "cand_789",
    "title": "Steven Avery: Innocent or Guilty?",
    "score": 0.43,
    "reasons": ["latin token overlap too low", "no year evidence"]
  }
}
```

必须支持的事件：

```text
task.created
task.started
task.progress
task.failed
task.completed
item.discovered
item.parsed
candidate.found
candidate.accepted
candidate.rejected
plan.created
plan.conflict
operation.started
operation.completed
operation.failed
artwork.downloaded
nfo.written
rollback.started
rollback.completed
```

### 数据存储

建议使用 SQLite，而不是只靠日志。

表设计：

```text
tasks
media_items
media_files
candidates
match_decisions
plans
operations
artwork_assets
nfo_outputs
aliases
settings_audit_log
events
```

关键收益：

- UI 刷新后任务不丢。
- 失败后可继续。
- 用户确认的匹配可复用。
- 每次整理可审计和回滚。

### 搜索与匹配服务设计

匹配服务应分三步：

1. QueryBuilder：生成多个查询词。
2. CandidateProvider：从 TMDB/Tavily/本地 NFO 拉候选。
3. CandidateScorer：评分并解释。

QueryBuilder 示例：

```text
原始文件名: [Anti-DEI] Ane Koi - Suki Kirai Daisuki - 01 [BDrip...]
解析标题:
  - Ane Koi - Suki Kirai Daisuki
  - Ane Koi Suki Kirai Daisuki
  - 姉恋
  - 好き嫌い大好き
```

Scorer 维度：

```text
title_similarity
alias_hit
year_match
media_type_match
episode_count_match
language_compatibility
external_source_confirmation
negative_keyword_penalty
```

低于阈值时，系统必须停在确认态。

### 文件计划生成

不要让 organizer 直接移动文件。它应该只生成计划。

示例：

```json
{
  "operations": [
    {
      "action": "mkdir",
      "destination": "/Media/这样高大的女孩子你喜欢吗？ (2026)/Season 01"
    },
    {
      "action": "move",
      "source": "/Downloads/[Sakurato] ... [03].mp4",
      "destination": "/Media/这样高大的女孩子你喜欢吗？ (2026)/Season 01/这样高大的女孩子你喜欢吗？ - S01E03.mp4"
    },
    {
      "action": "write",
      "destination": "/Media/这样高大的女孩子你喜欢吗？ (2026)/tvshow.nfo"
    }
  ]
}
```

### 安全执行器

执行器要求：

- 所有写入先写 `.tmp`。
- NFO 写完后 atomic rename。
- 图片下载完校验 MIME/尺寸后再 rename。
- 移动前确认 source 仍存在且 destination 未变化。
- 每个操作有 rollback action。
- 执行失败时自动暂停，不继续扩大破坏面。

### 后端测试设计

后端必须补测试，否则不适合继续扩功能。

测试矩阵：

```text
filename_parser:
  - 字幕组命名
  - 动漫集数
  - SxxEyy
  - 年份
  - 多音轨/多字幕

matcher:
  - 正确 CJK 候选低相似度仍接受
  - 英文错配必须拒绝
  - 年份不符必须拒绝
  - 手动 TMDB ID 跳过搜索

planner:
  - movie plan
  - tv season plan
  - loose files plan
  - conflict plan

executor:
  - move/copy/write
  - rollback
  - existing destination

artwork:
  - limit 生效
  - Extra 编号不覆盖
  - overwrite false 不覆盖
  - manifest 正确

nfo:
  - movie snapshot
  - tvshow snapshot
  - episode snapshot
```

## 前端完整设计蓝图

前端现在的问题不是“不好看”，而是信息架构不符合媒体管理器工作流。它应该从“点 RUN 看结果”变成“审计、确认、执行、恢复”的工作台。

### 前端目标

- 用户明确知道系统正在做什么。
- 每个匹配都能解释。
- 每个文件变更都能预览。
- 危险操作必须确认。
- 失败可以修正，而不是只能 retry。

### 页面结构

推荐主导航：

```text
Dashboard / 工作台
Library Scan / 扫描
Match Review / 匹配审核
Plan Review / 执行计划
Execution / 执行进度
History / 历史与回滚
Settings / 设置
Logs / 日志
```

当前三个 Tab 太少，导致审计、执行、日志、设置混在一起。

### 工作台 Dashboard

Dashboard 应显示：

- 当前任务状态。
- 最近任务。
- 待确认项目数。
- 失败项目数。
- 可回滚任务数。
- API 状态：TMDB、Tavily、LLM。

不应该只显示总数和运行状态。

### 扫描页面

扫描页控件：

```text
输入路径
媒体类型：Auto / Movie / TV
扫描模式：Auto / Single / Batch / Loose Files
搜索策略：TMDB Only / Smart(TMDB+Tavily) / Tavily Only
安全模式：只审计 / 生成计划 / 执行
```

扫描完成后显示 `MediaItem` 列表：

```text
文件/目录
解析标题
年份
媒体类型
集数
解析置信度
状态
```

### 匹配审核页面

这是最重要的页面。

每个媒体项显示：

- 文件原名。
- 解析标题。
- 推荐候选。
- 所有候选卡片。
- 分数和理由。
- 拒绝理由。
- 手动输入 TMDB ID。
- 记住别名。

候选卡片示例：

```text
这样高大的女孩子你喜欢吗？ (2026)
TMDB ID: 324120
Type: TV
Score: 0.82
Reasons:
  - Tavily confirmed TMDB page
  - TMDB original title CJK localized match
  - Episode count plausible
Warnings:
  - Romanized title similarity low
```

### 执行计划页面

计划页应是文件管理器的核心。

显示：

- 将创建的目录。
- 将移动/复制的文件。
- 将写入的 NFO。
- 将下载的图片。
- 冲突项。
- 危险项。

操作：

- 确认全部。
- 只确认高置信。
- 跳过低置信。
- 修改单项 destination。
- 选择冲突策略。

### 执行页面

执行时显示结构化进度：

```text
Scanning      12/12
Matching      10/12
Planning      10/10
Moving files   6/10
Writing NFO    4/10
Artwork        2/10
```

每个 task 支持：

- pause
- cancel
- retry failed
- rollback completed

### 历史与回滚页面

必须有历史页。

每条历史任务显示：

- 输入路径。
- 执行时间。
- 成功/失败数。
- 变更文件数。
- 是否可回滚。
- 查看计划。
- 查看日志。
- 回滚。

### 设置页面

设置页应分区：

#### API

- TMDB token
- Tavily key
- OMDB key
- LLM endpoint/key
- Test connectivity

密钥默认 mask，不明文展示。

#### Matching

- 置信度阈值。
- 年份严格模式。
- 是否允许低置信自动匹配。
- Tavily fallback 开关。
- 别名表管理。

#### Organization

- 命名模板：
  - Movie: `{title} ({year})/{title} ({year})`
  - TV: `{title} ({year})/Season {season:02d}/{title} - S{season:02d}E{episode:02d} - {episode_title}`
- 冲突策略。
- 是否重命名父目录。
- 是否创建 Season 文件夹。
- 默认 copy/in-place。

#### Artwork

- 主图下载开关。
- Extra 图下载开关。
- poster/backdrop/logo/still/actor 数量。
- 图片语言优先级。
- 覆盖策略。

#### Safety

- 默认 dry-run。
- 执行前二次确认。
- 自动生成 rollback log。
- 最大并发。

### 前端状态管理

当前状态散落在 `useState`、`localStorage` 和日志解析里。建议：

- 使用 TanStack Query 管理 API 数据。
- 使用 Zustand 管理 UI transient state。
- WebSocket event reducer 统一更新 task store。
- 表单使用 react-hook-form + zod。

状态结构：

```ts
interface TaskStore {
  tasks: Record<string, Task>;
  items: Record<string, MediaItem>;
  candidates: Record<string, Candidate[]>;
  plans: Record<string, OrganizationPlan>;
  operations: Record<string, FileOperation>;
}
```

事件 reducer：

```ts
function applyEvent(state: TaskStore, event: TaskEvent): TaskStore {
  switch (event.type) {
    case "item.discovered":
    case "candidate.found":
    case "operation.completed":
      return updatedState;
  }
}
```

### 前端组件设计

推荐组件：

```text
components/
  task/
    TaskTimeline.tsx
    TaskSummary.tsx
    TaskProgressBars.tsx
  scan/
    ScanForm.tsx
    PathPicker.tsx
    ScanModeControl.tsx
  match/
    MediaItemTable.tsx
    CandidateCard.tsx
    ScoreBreakdown.tsx
    ManualMatchDialog.tsx
  plan/
    PlanDiffTable.tsx
    ConflictBadge.tsx
    OperationRow.tsx
  artwork/
    ArtworkPreviewGrid.tsx
    ArtworkManifestView.tsx
  settings/
    ApiSettings.tsx
    MatchingSettings.tsx
    ArtworkSettings.tsx
    OrganizationSettings.tsx
```

### 前端 API Client

不要在组件里裸 `fetch`。

建议：

```text
client/src/api/
  client.ts
  tasks.ts
  settings.ts
  filesystem.ts
  matches.ts
  plans.ts
  events.ts
```

示例：

```ts
export async function createTask(input: CreateTaskInput): Promise<Task> {
  return api.post("/api/tasks", input);
}
```

### 前端 UX 细节

- RUN 按钮根据模式改名：`Scan`、`Generate Plan`、`Execute`。
- 整理模式应显示危险提示。
- 低置信候选用黄色，高风险操作用红色。
- 文件移动计划使用 diff 样式。
- 图片下载用缩略图网格展示。
- 错误不能只 toast，必须进入任务详情。
- retry 必须允许修改参数，不应盲目重跑。

### 前后端契约

前端和后端应共享 schema。

可选方案：

- 后端 FastAPI OpenAPI 自动生成 TypeScript types。
- 或维护 `schemas/`，Python Pydantic 和 TS Zod 双向生成。

必须保证：

- API 字段命名稳定。
- 事件类型稳定。
- 前端不依赖日志文本。
- 失败结构统一。

统一错误格式：

```json
{
  "error": {
    "code": "MATCH_LOW_CONFIDENCE",
    "message": "No candidate passed confidence threshold",
    "details": {
      "query": "Front Innocent",
      "best_candidate": "Steven Avery: Innocent or Guilty?",
      "score": 0.43
    }
  }
}
```

## 端到端工作流设计

### 审计工作流

```text
用户选择路径
  -> 创建 task
  -> scan_service 发现 items
  -> parse_service 解析标题/集数
  -> match_service 生成候选
  -> UI 展示候选和置信度
  -> 用户确认或修正
```

### 整理工作流

```text
已确认候选
  -> plan_service 生成计划
  -> UI 展示文件 diff
  -> 用户确认
  -> executor 执行
  -> verifier 校验文件/NFO/图片
  -> 写 operation log
```

### 失败修复工作流

```text
任务失败
  -> UI 显示失败阶段
  -> 用户选择：
       修改标题
       修改媒体类型
       输入 TMDB ID
       改用 Tavily
       跳过
  -> retry item
```

### 回滚工作流

```text
选择历史任务
  -> 查看 operations
  -> 生成 rollback plan
  -> 用户确认
  -> reverse executor 执行
  -> 校验
```

## 前后端重构优先级

### 第一批：必须先做

- 后端结构化事件替代日志解析。
- 前端 TaskBoard 改为事件 reducer。
- 后端引入 Task/Item/Candidate/Plan 模型。
- RUN 拆成 Scan / Generate Plan / Execute。
- 设置 API mask key。

### 第二批：安全整理

- plan_service。
- executor。
- operation log。
- rollback。
- conflict handling。

### 第三批：高质量刮削

- candidate scorer。
- alias database。
- artwork manifest。
- NFO snapshot tests。
- 图片排序策略。

### 第四批：产品化

- 历史页面。
- 失败修复页面。
- 批量确认。
- 设置 schema。
- OpenAPI -> TypeScript types。

## 判断项目是否“优秀”的验收标准

- 任意批量处理前，都能看到完整计划。
- 每个自动匹配都有可解释置信度。
- 低置信匹配不会自动整理。
- 所有文件移动都能回滚。
- NFO 输出有 snapshot tests。
- 图片下载有 manifest 和完整性校验。
- 前端状态来自结构化事件，不依赖日志正则。
- 配置保存后立即明确生效，不存在前后端配置错位。
- 失败任务可以手动修正并继续，不需要重跑全部。

## 最严厉但真实的结论

当前项目已经能“跑”，但还不能“信”。对个人小批量媒体目录，它可以作为辅助工具；对长期维护的真实媒体库，它需要先解决可信匹配和事务化整理，否则越自动越危险。

下一步最值得投入的不是再加更多搜索源，而是建立“候选解释 + 执行计划 + 回滚”的骨架。只要这三件事做好，TMDB、Tavily、图片下载、NFO、前端体验都会自然变得稳定。
